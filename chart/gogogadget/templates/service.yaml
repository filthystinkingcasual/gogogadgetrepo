apiVersion: v1
kind: Service
metadata:
  name: {{ include "gogogadget.fullname" . }}
  labels:
    {{- include "gogogadget.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gogogadget.selectorLabels" . | nindent 4 }}
